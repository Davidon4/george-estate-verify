export interface SignupProps {
    email: string;
    password: string;
    fullName: string;
    houseNumber: number;
    houseType: string;
    block: string;
    color: string
}

export interface SigninProps {
    email: string;
    password: string;
}